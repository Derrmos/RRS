{
  "name": "rrs",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev:bot": "pnpm --filter bot dev",
    "dev:dashboard": "pnpm --filter dashboard dev",
    "dev": "pnpm run --parallel dev:bot dev:dashboard"
  },
  "devDependencies": {
    "prettier": "3.3.3"
  }
}
